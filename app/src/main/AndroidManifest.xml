<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rs.raf.projekat2.darko_dimitrijevic_rn9418">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".application.Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Projekat 2"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".presentation.view.activities.MainActivity" />
        <activity android:name=".presentation.view.activities.LoginActivity" />
        <activity android:name=".presentation.view.activities.CreateNoteActivity" />
        <activity android:name=".presentation.view.activities.EditNoteActivity" />

        <activity android:name=".presentation.view.activities.SplashActivity" android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


    </application>

</manifest>
